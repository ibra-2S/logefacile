import 'package:flutter/material.dart';

// palette de couleurs officielle de LogeFacile
class AppColors {
  // couleurs principales
  static const bleuFonce = Color(0xFF1A237E);
  static const bleuMoyen = Color(0xFF1565C0);
  static const bleuClair = Color(0xFFE3F2FD);

  // couleurs des rôles
  static const vertProprietaire = Color(0xFF2E7D32);
  static const bleuAgent = Color(0xFF1565C0);
  static const tealLocataire = Color(0xFF00695C);
  static const violetAdmin = Color(0xFF6A1B9A);

  // couleurs de statut
  static const succes = Color(0xFF2E7D32);
  static const erreur = Color(0xFFC62828);
  static const avertissement = Color(0xFFE65100);
  static const info = Color(0xFF1565C0);

  // couleurs neutres
  static const fond = Color(0xFFF5F7FA);
  static const blanc = Color(0xFFFFFFFF);
  static const grisClair = Color(0xFFECEFF1);
  static const grisMoyen = Color(0xFF78909C);
  static const grisFonce = Color(0xFF37474F);
  static const noir = Color(0xFF263238);

  // couleurs de texte
  static const texte = Color(0xFF263238);
  static const textSecondaire = Color(0xFF546E7A);
  static const texteLeger = Color(0xFF78909C);
}
